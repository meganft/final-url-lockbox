<div class="links-index">
<% @links.each do |link| %>
<div class="link" data-link-id="<%= link.id %>" data-link-url="<%= link.url %>">
  <h5>Title: <%= link.title %></h5>
  <p>URL: <%= link.url %></p>
  <p><span class="read-status">Read status: <%= link.read %></span>
    <% if link.read == false %>
    <span class="mark-as-read"><button type="button">Mark as Read</button></span></p>
    <% else  %>
    <span class="mark-as-read"><button type="button" disabled>Already Read</button></span></p>
    <% end %>
    <p> <%= link_to "Edit this Link", edit_link_path(link) %>
</div>
<% end %>
</div>

<h5>Make a new link:</h5>
<div class="new-link-form">
<%= form_tag links_path, :method => :post do %>
<%= label_tag :url %> <%= text_field_tag :url %></p>
<%= label_tag :title %> <%= text_field_tag :title %>

<%= submit_tag "Submit Link" %>
<% end %>
</div>




Nate's code from here down:
<form id="new-link">
  <label>URL:
    <input id="link-url">
  </label>
  <label>Title:
    <input id="link-title">
  </label>
  <input type="submit" value="Add Link">
</form>

<div id="links-list"></div>

<br>

<h2>Hot Reads!</h2>
<ol id="hot-reads">
<%# @hot_links.each do |read| %>
  <li class='hot-read'>
    <a href="<%#= read.url %>"><%#= read.url %></a>
  </li>
<%# end %>
</ol>
<img src="http://i.giphy.com/Wj2Qm3eaW1gGY.gif">
